version: '3.1'

services:
  be:
    build:
      context: ./humako
    ports:
    - 3010:3010
  db:
    build:
      context: ./postgres
    ports:
     - 5432:5432
  fe:
    build:
      context: ./frontend
    ports:
     - 80:80
